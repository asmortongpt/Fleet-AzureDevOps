export const initCollaborationService = async () => {
  // Stubbed for tests; real implementation not available in this environment
  return;
};

export const shutdownCollaborationService = async () => {
  return;
};
