# Fleet Management API - Test Environment Configuration Template
# Copy this file to .env.test for running tests
# Use FAKE/TEST credentials only - no production secrets!

# ============================================================================
# Microsoft Graph/Azure AD Test Credentials
# ============================================================================
# IMPORTANT: These should be test tenant credentials only
# Never use production Azure AD credentials in tests
AZURE_AD_CLIENT_ID=YOUR_TEST_AZURE_AD_CLIENT_ID
AZURE_AD_CLIENT_SECRET=YOUR_TEST_AZURE_AD_CLIENT_SECRET
AZURE_AD_TENANT_ID=YOUR_TEST_AZURE_AD_TENANT_ID
AZURE_AD_REDIRECT_URI=http://localhost:3000/auth/callback

# ============================================================================
# Microsoft Graph API Test Credentials
# ============================================================================
MICROSOFT_GRAPH_CLIENT_ID=YOUR_TEST_MSGRAPH_CLIENT_ID
MICROSOFT_GRAPH_CLIENT_SECRET=YOUR_TEST_MSGRAPH_CLIENT_SECRET
MICROSOFT_GRAPH_TENANT_ID=YOUR_TEST_MSGRAPH_TENANT_ID

# ============================================================================
# Test Database Configuration
# ============================================================================
# Use a separate test database - NEVER point to production!
DATABASE_URL=postgresql://test_user:test_password@localhost:5432/fleet_test
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fleet_test
DB_USER=test_user
DB_PASSWORD=test_password

# ============================================================================
# JWT Secret for Test Tokens
# ============================================================================
# This can be a simple string for testing - not used in production
JWT_SECRET=test_jwt_secret_for_security_tests_only

# ============================================================================
# Node Environment
# ============================================================================
NODE_ENV=test

# ============================================================================
# SECURITY NOTES FOR TEST ENVIRONMENT
# ============================================================================
# 1. Use a separate Azure AD test tenant
# 2. Test database should contain NO real user data
# 3. Test credentials should have minimal permissions
# 4. Never reuse production secrets in test environment
# 5. Consider using mock services instead of real APIs
# ============================================================================
