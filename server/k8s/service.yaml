apiVersion: v1
kind: Service
metadata:
  name: fleet-api-service
  namespace: fleet-management
  labels:
    app: fleet-api
spec:
  type: ClusterIP
  selector:
    app: fleet-api
  ports:
  - name: http
    port: 3000
    targetPort: 3000
    protocol: TCP
  sessionAffinity: None
