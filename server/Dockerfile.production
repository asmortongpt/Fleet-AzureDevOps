# ============================================================================
# Fleet Management Backend API - Production Dockerfile
# ============================================================================
# Multi-stage build for optimized production image
# Security-first: non-root user, minimal dependencies, vulnerability scanning
# ============================================================================

# ============================================================================
# Stage 1: Builder - Compile TypeScript and install dependencies
# ============================================================================
FROM node:20-alpine AS builder

# Set working directory
WORKDIR /app

# Install build dependencies (python3, make, g++ for native modules like bcrypt)
RUN apk add --no-cache \
    python3 \
    make \
    g++ \
    linux-headers

# Copy package files first (layer caching optimization)
COPY package*.json ./
COPY tsconfig.json ./

# Install ALL dependencies (including devDependencies for build)
# Use npm ci for reproducible builds
RUN npm ci --include=dev

# Copy source code
COPY src/ ./src/

# Build TypeScript to JavaScript
RUN npm run build

# Prune devDependencies to reduce image size
RUN npm prune --production

# ============================================================================
# Stage 2: Production - Minimal runtime image
# ============================================================================
FROM node:20-alpine

# Install dumb-init for proper signal handling (PID 1 problem)
# dumb-init ensures SIGTERM is properly forwarded to Node.js process
RUN apk add --no-cache dumb-init

# Create non-root user for security (NEVER run as root in production)
RUN addgroup -g 1001 -S nodejs && \
    adduser -S nodejs -u 1001

# Set working directory
WORKDIR /app

# Copy built application and production dependencies from builder stage
COPY --from=builder --chown=nodejs:nodejs /app/dist ./dist
COPY --from=builder --chown=nodejs:nodejs /app/node_modules ./node_modules
COPY --from=builder --chown=nodejs:nodejs /app/package*.json ./

# Health check endpoint (used by Azure Container Apps)
# Checks /health endpoint every 30 seconds
# Fails after 3 consecutive failures
# Starts checking after 30 second grace period
HEALTHCHECK --interval=30s --timeout=5s --start-period=30s --retries=3 \
  CMD node -e "require('http').get('http://localhost:3000/health', (res) => { \
    process.exit(res.statusCode === 200 ? 0 : 1); \
  }).on('error', () => process.exit(1));"

# Switch to non-root user
USER nodejs

# Expose port 3000
EXPOSE 3000

# Use dumb-init to handle signals properly (prevents zombie processes)
ENTRYPOINT ["dumb-init", "--"]

# Start the application
# Uses Node.js directly (not npm start) for faster startup
CMD ["node", "dist/index.js"]

# ============================================================================
# Build Arguments (optional, can be overridden at build time)
# ============================================================================
# Example: docker build --build-arg NODE_ENV=production -t fleet-api .
ARG NODE_ENV=production
ENV NODE_ENV=$NODE_ENV

# ============================================================================
# Security Notes:
# ============================================================================
# 1. Multi-stage build reduces attack surface (no build tools in final image)
# 2. Non-root user (nodejs:1001) prevents privilege escalation
# 3. dumb-init handles signals properly (clean shutdown on SIGTERM)
# 4. Health check enables Azure Container Apps to detect unhealthy containers
# 5. Minimal alpine base image reduces vulnerabilities
# 6. All secrets passed via environment variables (NEVER hardcoded)
# ============================================================================

# ============================================================================
# Build Instructions:
# ============================================================================
# docker build -f Dockerfile.production -t fleetacr.azurecr.io/fleet-api:latest .
# docker push fleetacr.azurecr.io/fleet-api:latest
# ============================================================================
