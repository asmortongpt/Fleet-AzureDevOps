FROM node:20-alpine

WORKDIR /app

# Install only socket.io (no full package.json)
RUN npm install socket.io@4.8.1

# Copy emulator
COPY radio-emulator.js .

EXPOSE 8082

USER node

CMD ["node", "radio-emulator.js"]
