[pytest]
testpaths = tests/api/python
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --strict-markers
    --tb=short
    --maxfail=10
markers =
    integration: Integration tests
    api: API tests
    smoke: Smoke tests
    slow: Slow running tests
