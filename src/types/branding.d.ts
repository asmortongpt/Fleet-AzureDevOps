/**
 * Interface representing the structure of a tenant's branding configuration.
 */
export interface BrandingConfig {
  logoUrl: string;
  primaryColor: string;
  secondaryColor: string;
  fontFamily: string;
}