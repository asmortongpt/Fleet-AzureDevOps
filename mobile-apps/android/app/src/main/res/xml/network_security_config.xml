<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Base configuration for all connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Development/Debug exception for localhost only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">10.0.2.2</domain> <!-- Android emulator host -->
    </domain-config>

    <!-- Production domain configuration -->
    <domain-config>
        <domain includeSubdomains="true">fleet.capitaltechalliance.com</domain>
        <domain includeSubdomains="true">capitaltechalliance.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
        <!-- Enforce certificate pinning for production (optional but recommended) -->
        <!-- Uncomment and add your certificate pins -->
        <!--
        <pin-set expiration="2026-01-01">
            <pin digest="SHA-256">YOUR_CERTIFICATE_PIN_HERE</pin>
            <pin digest="SHA-256">YOUR_BACKUP_PIN_HERE</pin>
        </pin-set>
        -->
    </domain-config>
</network-security-config>
