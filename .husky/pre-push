#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running pre-push quality gates..."

# Run unit tests
echo "🧪 Running unit tests..."
npm run test:unit || {
  echo "❌ Unit tests failed. Please fix before pushing."
  exit 1
}

# Run coverage check
echo "📊 Checking test coverage..."
npm run test:coverage || {
  echo "❌ Coverage threshold not met. Please add more tests."
  exit 1
}

# Run linting
echo "🔍 Running linter..."
npm run lint || {
  echo "❌ Linting errors detected. Please fix before pushing."
  exit 1
}

# Run build check
echo "🏗️ Verifying build..."
npm run build || {
  echo "❌ Build failed. Please fix before pushing."
  exit 1
}

echo "✅ All pre-push checks passed!"
